name: Recipe
description: Defines a simple recipe field type.

# The type must be 'module' and the package must be 'Field types'
type: module
package: Field types
core: 8.x
version: 1.1

# The list of dependencies must contains the 'field' value
dependencies:
  - field
